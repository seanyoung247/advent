
function formatData(input) {
    return JSON.parse(input.filter(e=>e));
}

class Pair {
    constructor(data) {
        this.data = data;
        this.left = null;
        this.right = null;
    }
}

class SnailFishNumber {
    constructor() {
        this.root = null;
    }

    insert() {}
}

function buildTree(number) {

}

function add(n1, n2) {}

function explode(number) {}

function split(number) {}

function reduce(number) {}

function debugStr(number) {}

export class Solutions {
    one(input) {
        const numbers = formatData([...input]);

    }

    two(input) {
        let result = 0;
        // SOLUTION TWO CODE HERE
        return result;
    }
}
